# Installed Pi probe canonical root admission

This **foundation** correction changes only the installed Pi probe and its focused regression file. Source base: `7bf385f600782daaa2c688604735bb10ff776437`. [Source inputs](source-inputs.json) pin both branches; [the lossless source patch](source-change.patch.gz) reconstructs this branch's changes. Every other listed production source remains identical to the pinned base.

The prior macOS ARM job [105377132612](https://github.com/hashgraph-online/hol-guard/runs/105377132612) checked out merge `eafc8038f2d3051991768d944e0bd855fdcc5889`, merging head `26cd4dff3f138990a8e9f6a729a970c1dbd90fa4`; the native runtime reports the same merge build SHA. The default-auto stage completed **21 of 21 native-resident decisions**, with zero Python semantic, oneshot or fail-safe decisions. The following distinct Pi-output stage failed with `installed Guard daemon workspace policy was not ready`. [Hosted identity and exact excerpts](hosted-failure.json) and [the full raw job log](hosted-job-105377132612.log.gz) retain this outcome. No error code was exported by that readiness failure; the local alias witness establishes a matching source defect without inventing a missing hosted error field.

The Pi probe explicitly creates a private root under `/tmp`, then bypasses HTTP workspace admission by registering and preparing its derived workspace directly. On an aliased temporary parent that supplies a lexical workspace to scoped capture. The correction calls `root.resolve()` **once**, inside the existing cleanup try immediately after temporary-root creation and before deriving home, guard home or workspace. All registration, preparation, positive/negative route paths and cleanup use that admitted root. No capture is re-resolved later; no production module, threshold, five-second readiness deadline, capture identity, held-scope validation or ACK-withdrawal rule changes.

Two new parameterized cases, for plain and aliased roots, exercise actual `_run_probe` → `_start_installed_daemon` registration → `_prepare_installed_daemon_workspace` through a real `HookConfigReadScope` and publisher. They prove capture succeeds, the same canonical workspace reaches the Node handoff, a later workspace retarget is rejected and withdraws the simulated prior ACK, exact capture identity remains, and cleanup uses the same canonical root. With controlled time 10 the actual helper passes deadline 15. Daemon/native/Node execution is substituted; the synthetic preparation result is a capture-boundary fixture, not native ACK, IPC, generated-extension or installed-platform qualification. These added witnesses explicitly skip on Windows because `_run_probe` is POSIX-only, and skip alias creation where unsupported. Both executed on this Linux host.

Final exact-source check: **32 tests passed** in [the complete Pi test file command](foundation-tests-posix.json). Ruff and formatting pass for the two edited files. Earlier runs are retained and are not summed: the pre-fix main alias witness fails after actual `guard_config_scope_changed` (wrapped as `GuardConfigSourceError` readiness failure), then its cleanup assertion also observes the lexical guard-home alias. The first post-fix suites each report 30 passed / 2 failed because the new test stub returned a string rather than a `Path` for the interpreter; that fixture was corrected. The next suites pass 32; the final suites repeat after the reviewer-requested POSIX-only marker. [The original regression bytes](initial-regression.py.txt.gz) preserve the first attempts exactly.

Focused types **do not pass**: each corrected probe has **1 file, 3 errors, 158 warnings**. An exact original-probe snapshot, checked with the same main project configuration, also has 3 errors / 158 warnings. [The comparison](type-baseline-comparison.json) verifies all 161 diagnostic severity/message/rule/ranges and order are identical after removing the two added line offsets and source filename. The existing errors concern `_SimplePath` use with `lstat`/`resolve` and possibly unbound `guard_home` in cleanup. They were not changed or hidden. [Original source](baseline-probe.py.gz), baseline command/output and both candidate outputs are retained. This is a bounded one-file comparison, not a full-repository type qualification.

[Independent read-only review](independent-review.json) found no production blocker and identified the addressed Windows test skip. All logs and patches use lossless gzip; the manifest records stored and decoded sizes/hashes, while command receipts retain the original stdout sizes/hashes and timestamps. Heavy checks used `flock --close -w 180` on the common lock; wrapper elapsed time includes lock acquisition. No full qualification campaign or hosted rerun is claimed by this local receipt.
