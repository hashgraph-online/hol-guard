# Desktop source-binding refresh

The complete 51,000-case generator independently regenerated this branch’s report. Its exact --check passed. Only five expected runtime source hashes changed; every other decoded report, decision, oracle and corpus field is identical to the prior fixture. Comparison and source-hash receipts are retained.

Foundation associated tests passed 15 and failed 1 unchanged 45-second budget assertion. Both fresh-process report digests matched; the original failure remains. The exact second elapsed value was elided by pytest and is not reconstructed or replaced.

The original cdd hosted stale-fixture failure is retained with exact decoded log bytes (gzip). The actual hosted test-merge has the same tree as cdd. This local correction does not claim fresh hosted success or any security-alert disposition. No product, oracle, deadline or threshold was changed.
