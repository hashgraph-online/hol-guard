## Summary
- Combine the two review alerts (“Guard paused this so you can review it first.” and the secondary risk summary such as “HOL Guard blocked `package` before install.”) into a single consolidated alert with a Next button.
- Extend `ConsolidatedEvidenceAlert` to accept an optional per-item icon so the paused reason keeps the info icon and the risk summary keeps the warning icon.
- Move these two text alerts above the “What would happen without Guard?” section; richer evidence cards remain inside the existing Review details collapsible.

## Testing
- `cd dashboard && npm test`
- `cd dashboard && npm run build`

## Notes
- Pre-existing TypeScript errors in the dashboard are unrelated to this change.
