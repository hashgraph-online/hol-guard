[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "benign-python-package"
version = "1.0.0"
description = "A benign Python package with static metadata."
