apiVersion: guard.hashgraphonline.com/v1alpha1
kind: GuardPolicy
metadata: &metadata
  id: policy.invalid
  name: Invalid
  revision: 1
spec:
  defaults:
    mode: prompt
  rules: []
x-copy: *metadata
