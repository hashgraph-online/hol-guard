# Installed default-auto probe workspace admission

This is the **foundation** receipt for the probe-only canonical workspace admission fix. The source baseline is `bf0c85d2372a904b3c228415cc9eaae8c8948c6f`. Exact final source hashes for both branches are in [source-inputs.json](source-inputs.json); [probe-change.patch.gz](probe-change.patch.gz) (lossless gzip; decompress before applying) reconstructs this branch's probe from its baseline. The new regression file is committed alongside this receipt. No production module is changed by this correction.

The direct worker probe created its workspace beneath a potentially aliased temporary parent and registered that lexical path. Production hook ingress admits a canonical workspace first. Scoped capture deliberately rejects a later difference from the admitted parent: the local socket-free witness produces `guard_config_scope_changed`, and actual `_publish_once` records `guardconfigsourceerror` while keeping ACK false. `prepare_workspace_policy` skips readiness waiting after a publisher error, which explains the demonstrated fast rejection. This is a concrete alias-path mechanism; the historical hosted logs did not export the publisher error and do not prove that it caused every hosted failure. The Windows cause remains unproved.

The correction resolves the newly created workspace exactly once, before registration/preparation, then retains that same value for the route corpus. It adds only a fixed-code `publisher_last_error` diagnostic on failed readiness: unknown text, subclasses and non-string values map to `other`; `None` stays `None`. It changes no readiness threshold, deadline, capture identity, held-parent authorization, publisher registry selection or ACK-withdrawal rule. The configured guard-home handling and branch-specific fixture features remain intact.

The eight added cases exercise the real scope and publisher against a directory alias, successful capture after admission, rejection and ACK withdrawal after retargeting that admitted path, fixed-code/privacy filtering, cleanup and rejection of a snapshot returned after the original deadline. The fake worker's successful binding is deliberately synthetic: the test does not run native IPC or claim resident ACK qualification. The alias case may skip on a platform without directory-symlink permission; it did execute in this Linux run.

Final focused results: **35 passed**, no failures or skips, in the exact files listed in [foundation-tests-final.json](foundation-tests-final.json). The initial focused run is retained separately and is not added to the final count. Both initial and final runs cover the same cases. The rerun follows only removal of an unused test import and an exception-class suffix correction after the retained initial Ruff failure; probe bytes were unchanged. Final Ruff and format checks pass for two files. Focused basedpyright analyzes only the probe: **1 file, 0 errors, 43 warnings**. This is not a full-repository type result. Every raw command receipt records the command, start/end timestamps, exit code and output hash; elapsed wrapper time includes shared-lock acquisition. Heavy checks used the common lock with `flock --close -w 180`.

[alias-witness.py.txt](alias-witness.py.txt) retains the exact diagnostic program; [foundation-alias-witness.log](foundation-alias-witness.log) is its complete output, executed once against the foundation source recorded in [source-inputs.json](source-inputs.json). It starts no daemon thread, socket or native client. The publication context uses a synthetic native-status provider and a client that raises if called. It confirms lexical rejection, successful canonical admission with the identical capture object, and rejection of a later retarget. The actual regression also exercises this branch's scope/publisher. The archived script has a historical absolute foundation source path; use that recorded tree or replace only this local checkout path when reproducing elsewhere. [run-check.py.txt](run-check.py.txt) retains the command-output capture helper.

[hosted-failure-inputs.json](hosted-failure-inputs.json) records the retained raw-log hashes and exact failure lines for macOS Intel job 105364319651, macOS ARM job 105364319920 and Windows job 105364320017. Those prior jobs reported readiness false in 0.06/0.06/0.0 ms. The new diagnostic was absent there. This local receipt does not replace those outcomes, claim a hosted platform pass, rerun installed qualification or relax any deadline.

[Independent read-only review](independent-review.json) of the main probe and shared regression file found no concrete blocker in the admission, privacy or deadline boundaries; it was not a separate foundation review or test run. The manifest hashes all retained artifacts except itself. Reproduce the focused command from the final tests receipt and the lint/type commands from their receipts, selecting this source tree and its dependencies. No GitHub action, security-alert disposition, production config change or benchmark campaign is part of this receipt.

The final whole-change whitespace preflight initially failed because blank context lines in the archived unified patch contain the significant single-space diff marker. [The original failed command and exit 2](packaging-preflight-failed.json) and its complete losslessly compressed stdout are retained. Only artifact packaging was corrected: the exact original patch bytes are now gzip-compressed, with stored and decoded byte counts and SHA-256 values in the manifest. [The original manifest](manifest-before-packaging.json) is preserved byte-for-byte. Probe and test source bytes are unchanged, and no tests or type checks were rerun for this packaging correction. The staged whole-change check is recorded separately; the containing commit is also checked after commit.
