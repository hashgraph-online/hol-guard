apiVersion: guard.hashgraphonline.com/v1alpha1
kind: GuardPolicy
metadata:
  id: policy.invalid
  name: Invalid
  revision: 1
spec:
  defaults:
    mode: prompt
  rules:
    - id: rule.invalid
      enabled: true
      effect: block
      match:
        packages: []
      lifetime:
        mode: permanent
      provenance:
        source: import
        createdAt: 2026-07-15T12:00:00Z
