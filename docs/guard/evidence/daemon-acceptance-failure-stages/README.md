# Acceptance fixture failure-stage diagnostic

This is the **foundation** test-fixture receipt. It adds a finite `failure_stages` counter to the diagnostic `WorkloadResult`; it does not fix or explain the historical transport failure.

The original foundation job `105377523075`, source `26cd4dff3f138990a8e9f6a729a970c1dbd90fa4`, ran through test merge `eafc8038f2d3051991768d944e0bd855fdcc5889`. Its mixed-harness case retained 214 routine allows and 24 secret denials from 240 logical requests, with two `RemoteDisconnected` exceptions and only 58 Claude dispatches. The shard had 201 passes and one failure in 128.03 seconds. The complete original log is stored losslessly, with exact decoded byte hashes in [hosted-before.json](hosted-before.json). The archived foundation fixture equals the actual hosted source bytes. No historical main failure is inferred.

Both fixture branches use a connection for identity challenge and then hook exchange. Their original error counters did not identify which phase raised. The new counter has only four fixed labels: `request_setup`, `identity_challenge`, `hook_exchange` and `response_classification`. The existing exception handler increments it under the existing lock. Original exception-reason keys, dispatch and outcome accounting remain unchanged. Main and foundation keep their distinct existing scheduling, admission/retry and timeout control flow; this change does not copy one branch's policy into the other.

The counter covers **caught exceptions only**. Policy-derived generic failures or capacity outcomes need not add an entry. A `hook_exchange` entry means the client entered that phase, including its preparation; it is not proof that the daemon received or executed a hook. The old logs do not establish the challenge/hook stage or the production cause. No config-scope, overload, flakiness or server-trace attribution is made.

The counter is the final dataclass field, visible in returned results and failure-assertion `repr`; the tests verify that placement. New diagnostics contain only fixed phase names and integer counts, without authentication headers, request text or payload values. The existing report sanitizer is unchanged and does not widen its export allowlist for this field.

The new socket-free tests drive the actual fixture with fake daemon/HTTP boundaries. They inject the same disconnect separately during challenge and hook, plus a classifier exception, for Codex and Claude. They verify exact connection/request counts, no ambiguous replay, original exception reasons, connection/daemon cleanup, correct success/failure conservation, and observable stage counters. The success cases preserve empty counters and normal allow/deny accounting. They start no real daemon, worker or socket and do not run native IPC or installed qualification.

Final focused validation: **8 passed in 0.69 seconds**. Ruff and format checks pass for the two owned files. Commands ran under the shared lock; wrapper duration includes lock/process startup and is separate from pytest's elapsed value. [focused-tests.json](focused-tests.json) pins the exact command and before/after bytes of both owned files. Those files stayed unchanged during each command; this is not a claim that the entire shared checkout was frozen. No full fairness, soak, performance campaign, repository-wide type gate or hosted correction pass was run.

The shared new test file was formatted in main before this branch ran any checks. This branch retains its own successful checks and source hashes; main retains the initial format-check failure and exact before-format bytes.

[diagnostic-only-proof.json](diagnostic-only-proof.json) records an exact AST reconstruction of this branch's complete prior fixture: removing only the added result field, counter, five phase assignments, one counter increment and one return keyword yields the original AST. [fixture-before.py.gz](fixture-before.py.gz) preserves its original bytes. The source/test files are committed with this evidence; the original source head and owned hashes are recorded separately. [run-check.py.txt](run-check.py.txt) preserves the capture driver with its original local checkout and lock paths. `gzip -dc` restores every compressed artifact byte for byte. No production file or acceptance threshold changes are part of this correction.
