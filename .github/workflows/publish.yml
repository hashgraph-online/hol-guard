name: Release 3.1 publisher retired

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    name: Reject retired release train
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Reject release publication
        run: |
          echo "The release/3.1 publisher is retired."
          exit 1
